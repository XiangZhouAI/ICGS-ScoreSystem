import React from 'react';
import { Card } from '../Common/Card';

export const ScoringEntry: React.FC = () => {
  return (
    <Card title="Live Scoring Entry">
      <p>Scoring entry component coming soon...</p>
    </Card>
  );
};