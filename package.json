{
  "name": "icgs-score-system",
  "version": "1.0.0",
  "description": "Golf Competition Management and Scoring Application for Ireland Chinese Golf Society",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && nodemon index.js",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd client && npm install && cd ../server && npm install",
    "start": "cd server && node index.js"
  },
  "keywords": ["golf", "scoring", "stableford", "competition", "react", "mongodb"],
  "author": "Ireland Chinese Golf Society",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}